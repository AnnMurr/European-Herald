import { RenderCards } from "../../../../../../reusable/paginationCards/renderCards";

export const Cards = () => {
    return (
        <>
            <RenderCards type={'foundСards'} />
        </>
    )
}